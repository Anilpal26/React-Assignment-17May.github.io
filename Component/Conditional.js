const Conditional =()=>{
    const flag = true;
        return (
            <div>
            {flag ? <h2>This is Basic React</h2>: <p>Flag is false</p>}
            <button>Click Here to Learn More</button>
            </div>
        )
    }